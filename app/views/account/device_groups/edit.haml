%ol.breadcrumb
  %li 设备中心
  %li= link_to "分组管理", url_for("/")
  %li= link_to @record.name, url_for("/#{@record.id}")
  %li 编辑分组

= haml :_form

:javascript
  window.DeviceGroup = {
    onsubmit: function() {
      try {
        var objects = {device_group: { id: "#{@record.id}", name: $("#device_group_name").val() }}
        window.OperationLogger.record("device_group.edit", objects);
      } catch(e) { console.log(e) }

      $("#recordForm").submit();
    }
  }
