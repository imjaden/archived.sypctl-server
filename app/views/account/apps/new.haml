%ol.breadcrumb
  %li 应用中心
  %li= link_to "应用管理", url_for("/")
  %li 新建应用

= haml :_form

:javascript
  window.AppObject = {
    onsubmit: function() {
      try {
        var objects = {app: { id: "-1", name: $("#app_name").val() }}
        window.OperationLogger.record("app.new", objects);
      } catch(e) { console.log(e) }

      $("#recordForm").submit();
    }
  }