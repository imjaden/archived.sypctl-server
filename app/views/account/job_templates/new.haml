%ol.breadcrumb
  %li 部署中心
  %li
    %a{href: "#{url_for('/')}?page=#{params[:page] || 1}"} 模板管理
  %li
    新建模板

= haml :_form

:javascript
  window.JobTemplate = {
    onsubmit: function() {
      try {
        var objects = {job_template: { id: "-1", name: $("#job_template_title").val() }}
        window.OperationLogger.record("job_template.new", objects);
      } catch(e) { console.log(e) }

      $("#recordForm").submit();
    }
  }
